const base = 'https://pokeapi.co';
const api = 'api/v2';
const POKEMON_SOURCE = 'pokemon';
const TYPE_SOURCE = 'type';

const POKE_API_URL = `${base}/${api}`;

module.exports = { POKE_API_URL, POKEMON_SOURCE, TYPE_SOURCE };
